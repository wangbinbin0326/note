#include <stdio.h>
#include <math.h>
#include <ctype.h>
//分段函数求值
int main(void)
{
    char c;
    c = '7';
    printf("%d", isalpha(c));
    //printf("\nResult when uppercase alphabet is passed: %d", isalpha(c));
    return 0;
}